{
  "name": "trakt-manage",
  "version": "2.0.0",
  "module": "src/cli/index.ts",
  "type": "module",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.30.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "drizzle-orm": "^0.39.0"
  },
  "scripts": {
    "build": "bun build ./src/cli/index.ts --compile --outfile trakt",
    "dev": "bun run src/cli/index.ts",
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio"
  }
}
